// On importe la librairie (le nom doit correspondre à celui configuré dans "Manage Jenkins > System > Global Pipeline Libraries")
@Library('jenkins-shared-library') _

standardBackendPipeline(
    appName: 'authorization-service',
    registry: 'yassinekamouss'
)